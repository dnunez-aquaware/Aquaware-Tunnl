# .github/workflows/test-pr.yaml
name: Test PR Trigger
on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR trigger funciono correctamente"