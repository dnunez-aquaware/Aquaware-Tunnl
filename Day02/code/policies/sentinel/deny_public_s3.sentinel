import "tfplan/v2" as tfplan

main = rule {
    all tfplan.resource_changes as _, rc {
        rc.type != "aws_s3_bucket_public_access_block" or (
            (rc.change.after.block_public_acls else false) and
            (rc.change.after.block_public_policy else false))
    }
}